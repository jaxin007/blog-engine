export interface EnvConfigInterface{
  JWT_SECRET_KEY: string;
  POSTGRES_PASSWORD: string;
  PGHOST: string;
  PGUSER: string;
  PGDATABASE: string;
  PGPASSWORD: string;
  PGPORT: number;
  PORT: number;
}
